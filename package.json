{
  "name": "ai-job-application-drafter",
  "version": "0.1.0",
  "private": true,
  "description": "AI drafts your answers to the open-ended questions on job application forms. Chrome extension, your own OpenAI key, your resume stays on your machine.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "fixture": "node fixture.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2"
  }
}
